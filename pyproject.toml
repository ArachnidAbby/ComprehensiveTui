[project]
name = "pytui"
version = "0.1.0"
description = "A Text UI library similar to QT"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
