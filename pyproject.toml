[project]
name = "comprehensivetui"
version = "0.1.0"
description = "A Text UI library similar to QT"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
